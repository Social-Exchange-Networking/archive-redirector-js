# archive-redirector-js
JavaScript utility that fetches a JSON list of URLs on page load and redirects each entry to a randomly selected web-archive (Wayback Machine, Megalodon, GhostArchive, Archive.Today). Lightweight, configurable and privacy-aware client-side redirector for preserving and sharing archived links.
